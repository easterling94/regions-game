import { AppDispatch } from '../store';
import { setLives, setCorrect, setTimer } from '../slices/gameSlice';

export const changeTimerThunk = () => (dispatch: AppDispatch) => {

}

export const changeLivesThunk = () => (dispatch: AppDispatch) => {
  
}

export const changeCorrectThunk = () => (dispatch: AppDispatch) => {
  
}